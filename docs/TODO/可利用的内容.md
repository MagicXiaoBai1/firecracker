# 我们可以利用的firecracker

## 处理Guest对NPU相关的GPA的读写

vm.common.mmio_bus：  register_bars_with_bus(处理无法直通的bar空间)
pci_segment.pci_bus：  PCIe配置空间读写模拟
pci_segment.next_device_bdf()

## Guest内存管理部分

## 让kernel启动时看到NPU

fdt::create_fdt
fdt::create_pci_nodes
我们好像不需要这两个主要时配置pcie根节点的

## 中断控制

MsixConfig封装入VirtioInterruptMsix

```rust

let msix_vectors = Vm::create_msix_group(vm.clone(), msix_num)?;
let msix_config = Arc::new(Mutex::new(MsixConfig::new(
    msix_vectors.clone(),
    pci_device_bdf,
)));

msix_vectors是 Vec<MsixVector>
pub struct MsixVector {
    /// GSI used for this vector
    pub gsi: u32,
    /// EventFd used for this vector
    pub event_fd: EventFd,  // 去vfio申请
    /// Flag determining whether the vector is enabled
    pub enabled: AtomicBool,
}
```
